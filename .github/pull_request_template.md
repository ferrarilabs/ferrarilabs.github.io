## Related Issue

Closes #
<!-- Only use "Closes #NNN" if this PR fully resolves the Issue. If it's a partial fix,
     write "Relates to #NNN" instead and leave the Issue open. -->

## Summary

<!-- What changed, in 1-3 sentences. -->

## Root Cause

<!-- For bug/incident fixes: what was actually wrong, not just what was changed.
     N/A for pure features/docs. -->

## Changes

<!-- Bulleted list of the actual changes. -->

## Validation

<!-- How you confirmed this works: `npm run check` output, manual QA steps, screenshots.
     Per CLAUDE.md, `npm run check` must pass before this is considered done. -->

## Data Impact

<!-- Does this touch stored data (Supabase, localStorage, migrations)? If none, say "None". -->

## Scoring / Ranking Impact

<!-- Does this touch scoring, tiebreaks, ranking, entries, or payments in any bolão app?
     If none, say "None". If yes, this requires explicit authorization per CLAUDE.md —
     name it here. -->

## Risk

<!-- Blast radius if this is wrong: which app(s), production vs. not, reversibility. -->

## Rollback

<!-- Exact command/steps to revert this change if it needs to come out. -->

## Evidence

<!-- Links: audit script output, screenshots, related commits/PRs, CHANGELOG entry. -->
