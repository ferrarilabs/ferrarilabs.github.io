# Bugs and Feedback — Bolão do Ferrari

## Current version: v4.0-clean (2026-06-27)

---

## Known bugs (open)

| # | Description | Severity | Notes |
|---|---|---|---|
| B-01 | API-Football refresh does not auto-update `data.js` bracket data. Admin must manually reconcile. | Low | By design for now; avoids breaking validated bracket structure. |
| B-02 | Polymarket integration exists in config but the smart simulator does not visibly surface which market was matched or whether odds were used. | Low | Informational only; no user-facing display needed unless requested. |
| B-03 | Countdown timer hardcodes "dias/hrs/min/seg" in PT-BR regardless of active language. | Low | Labels are not in i18n. |
| B-04 | Data.js `flags` object has `Scotland` duplicated as a key (both `"Scotland":"🏴󠁧󠁢󠁳󠁣󠁴󠁿"` entries). | Cosmetic | Second entry silently overwrites first; no functional impact. |
| B-05 | Several Round of 32 team slots are still "3rd A/B/C/D/F" style placeholders because third-place group qualifiers are not yet determined. | Scheduled | Will resolve when group stage completes (Jun 27–28). |

---

## Fixed bugs — v4.0-clean (from v3.x history)

| Bug | Fixed in |
|---|---|
| CSV line endings were LF — broken in Excel on Windows | v3.0 (now CRLF) |
| Admin plaintext password comment left in source | v3.0 (removed) |
| Alternative fallback admin hash existed alongside primary | v3.0 (removed) |
| EmailJS `init` called without `limitRate` throttle | v3.0 (throttle: 30000ms) |
| Email payload included fields beyond `html_message` | v3.0 (reduced to `html_message` only) |
| Score validation ran only on display, not on actual save | v3.0 (validated in `readEntryFromForm`) |
| Score out of 0–20 range was accepted | v3.0 (parseScore clamps) |
| Winner/advance side inconsistency was not caught | v3.0 (validated before save) |
| Admin had no logout | v3.0 (logout added) |
| Admin could not delete entries | v3.0 (delete + email notification) |
| Admin payment/result events were per-element, not delegated | v3.0 (event delegation) |
| `scoreEntry` was called multiple times per ranking row | v3.0 (called once) |
| Participants list made multiple state reads | v3.0 (single read) |
| Receipt used `document.write` | v3.0 (Blob URL) |
| Simulator ran after cutoff and silently overwrote | v3.0 (blocked + confirm) |
| Dynamic team names were not HTML-escaped | v3.0 (escapeHtml applied) |
| Language dropdown was visible (UX issue) | v3.3.4 (replaced with flag buttons) |
| UK nation flag emojis were incorrect | v3.2.1 (Scotland/Wales/England fixed) |
| Initial "Time A / Time B" flash before teams resolved | v3.2.1 (score-label-A/B deferred) |
| `i18n-repair.js` patch file polluted global scope | v4.0 (removed; i18n fully in i18n.js) |
| PayPal payment method included but not used | v4.0 (removed) |
| Multiple legacy `FIX_LOG_*` patch files cluttered docs | v4.0 (removed) |

---

## User feedback received

All feedback is from Eduardo Ferrari (app owner, also primary tester) unless otherwise noted.

- **Timer:** requested seconds visible in countdown. Added in v3.3.4.
- **Language toggle:** requested flags instead of dropdown. Implemented in v3.3.1.
- **Receipt:** requested no popup-dependent flow. Implemented "Download HTML" fallback.
- **Rules section:** scoring table was missing; restored in v3.3.1.
- **Supabase:** requested optional remote sync so multiple devices share state. Added in v3.3.
- **Transparency:** requested disclaimer text about informal nature and receipt responsibility. Added to Rules section.
- **WhatsApp:** requested group link and QR accessible from the app. Added in v4.0.
- **Venmo/PayPal:** PayPal removed in v4.0 as it was not actually used.
- **Live scoreboard (suggested by Alan, a participant):** requested a live stream showing game results and points on the site (originally "live stream q mostra o resultado do jogo e a pontuacao"). Clarified as live score/points, not video. Added in v4.25: public auto-refreshing live score badge on the Jogos tab (ESPN, no admin login needed) + ranking auto-refresh every 90s.
- **Per-match points in picks table (Eduardo):** requested showing how many points each entry earned per match, not just the total. Added in v4.26: `picksTable` now shows the real result and points earned for every knockout match, alongside each pick.
- **Live points dropdown + match clock (Eduardo):** requested a dropdown under each live match showing everyone's provisional points at the current score, plus a minutes:seconds match clock instead of just the minute. Added in v4.27.
- **Mobile "not seeing changes" + ugly next-match countdown (Eduardo):** reported changes not showing on phone, and the next-match countdown timer wrapping badly on mobile. Root causes found and fixed in v4.31: stale `?v=` cache-busting params (stuck at 4.19 since several releases back) and a CSS specificity clash between `.count-grid`'s mobile 2-column override and the 3-cell next-match timer.
- **Rank-movement arrows on the live match points too (Eduardo):** requested the same up/down arrows as the main ranking, but scoped to each live match's provisional points table — like Globo Esporte's real-time Brasileirão table. Added in v4.32. Eduardo also confirmed: the database only updates when a match officially ends, never mid-game — the live points/arrows are purely a browser-side preview and never write to Supabase (already true by design; note text made more explicit about this in v4.32).
- **Live card redesign, Google style (Eduardo):** sent a screenshot of Google's live World Cup scoreboard (flag badge, team name below, big score, centered "Live" pill + running clock) and asked for the hero's live card to look like that. Redone in v4.33.
- **Live points list wasn't visible enough (Eduardo):** the per-match provisional points list existed but only behind a toggle on the Jogos tab — Eduardo expected it directly under the score on the hero live card. Moved there (always visible) in v4.34.
- **Tiebreaker rule finally decided (Eduardo), triggered by a WhatsApp screenshot of two entries tied for 1st with the joking caption "esse sistema é machista!":** cascade is (1) most exact scores, (2) most correct champion/runner-up/3rd picks, (3) if still tied, shared position — prize for that placement split manually since payouts aren't automated. Implemented consistently in the web ranking, the in-browser email builder, and the Python cron email script (`send_result_email.py`) in v4.36. Resolves ROADMAP M-01.
- **Exact-score counter in picks view (Eduardo):** wanted people to see, at the bottom of "Ver palpites," how many exact scores an entry got right — so ties in the ranking are self-explanatory against tiebreaker level 1. Added in v4.41.
- **Live clock "resets" when leaving and returning to the page, noticed during Spain's match (Eduardo):** root cause: ESPN's free feed lags real time (up to ~1-2 min, worse right after resuming from backgrounded), and a fresh poll's clockSeconds could be behind what the interpolated display already showed, making it jump backward. Fixed in v4.47 with a monotonic guard (`mergeLiveClock`) that keeps counting forward through small lag, while still allowing legitimate large resets (halftime, extra time). Note: `bolao-teste/` was renamed to `bolao/` in v4.44 by a separate concurrent session; this fix and future bolão work should target `bolao/js/app.js`.
- **Follow-up — clock still reset on every page refresh (Eduardo):** the v4.47 guard only had a "previous" value to compare against while the tab stayed open in memory; a full reload wiped it, so the first poll after refresh had nothing to protect against ESPN's laggy value. Fixed in v4.48 by persisting the last known clock per match to `localStorage`, so the guard survives reloads too.
- **Probability bar only in Jogos tab, wanted it live like Google/Coinbase/Polymarket (Eduardo):** the prob bar feature the Mac session shipped that morning only rendered inside the Jogos tab's match cards, not on the prominent hero live card. Added in v4.49 (shared `liveProbBarsHtml`, no duplicated logic). Also asked to pull the "real" probability from wherever Google sources it for their live scoreboard — there's no public access to whatever proprietary data Google uses; closest legitimate option is ESPN's own undocumented win-probability API, added as a best-effort upgrade with automatic fallback to the site's own Poisson model (confirmed via AskUserQuestion this was the preferred approach given the uncertainty).
- **Narrow prob-bar label overflow ("Austria 3%" spilling outside its sliver), noticed by Eduardo right after the above:** fixed same session — labels under 12% width are hidden, tooltip still carries the number.
- **Follow-up — don't hide the % too, just the name (Eduardo):** the first fix hid the entire label below 12%, leaving a blank sliver. Changed in v4.50 to always show at least "N%", dropping only the team/draw name when there's no room.
- **If ESPN's real probability doesn't pan out, use match stats instead (Eduardo):** suggested recalculating on goal changes (already happens every poll) or pulling match statistics as a hidden input to sharpen the estimate. Added in v4.51: shots-on-target/possession fetched from ESPN's summary endpoint, blended into the fallback Poisson lambdas (70/30) when the dedicated win-probability endpoint isn't available. Stats aren't shown anywhere in the UI, only used as calculation input.
- **Live points table positions out of order, caught via screenshot (Eduardo):** the "Pos." number (rank within the overall provisional standings) and the visual row order (sorted by this match's own points) used different tie-break rules when two entries were tied on provisional total, so a row labeled "3" could render above a row labeled "2". Fixed in v4.52 — both now use the identical comparator, rows sort directly by the same `provPos` shown to the user.
- **Rank movement arrows (Eduardo):** requested up/down arrows on the ranking showing whether an entry's position improved or dropped since the last score change, similar to a league table (referenced Globo Esporte's Brasileirão table). Added in v4.27.
- **Live points table positions out of order, caught via screenshot (Eduardo):** the "Pos." number (rank within the overall provisional standings) and the visual row order (sorted by this match's own points) used different tie-break rules when two entries were tied on provisional total, so a row labeled "3" could render above a row labeled "2". Fixed in v4.52 — both now use the identical comparator, rows sort directly by the same `provPos` shown to the user.
- **Goal scorer + minute in the live scoreboard, from a screenshot of Google's Spain vs Austria card (Eduardo):** wanted the same "M. Oyarzabal 36' / P. Porro 66'" list Google shows under the score. Added in v4.53 — pulled from the same ESPN scoreboard event already used for score/clock (`competitions[].details`), no extra request, shown on both the hero live card and the Jogos tab card. Best-effort like the probability features: renders nothing for a match if ESPN doesn't return that detail.
- **"None of the recent changes are live, even in incognito" (Eduardo), 2026-07-02:** root cause wasn't the code — GitHub Pages' own "Deploy to GitHub Pages" step failed silently for the v4.48–v4.51 merge (build succeeded, deploy step errored, most likely a race between the PR's squash-merge push and the `sync_version.yml` auto-bump push firing seconds apart). No error surfaces in this app when that happens; only visible by checking the repo's Actions tab. Fixed by re-running the failed deploy job. Worth a periodic sanity check after merges: confirm the "pages build and deployment" run for the latest commit actually shows a green checkmark, not just that the PR merged.
- **Match-end banner exposed an admin action to every visitor, caught via screenshot (Eduardo):** "Isso não pode aparecer para todos!!" — the public "Jogo encerrado!" banner (shows to anyone when a knockout match ends) included a "🔐 Admin → enviar emails" button even for logged-out visitors, which just navigated to the Admin section — surfacing an admin-only workflow to the whole bolão. Fixed in v4.54: non-admin visitors now only see the plain "Jogo encerrado!" notice with a dismiss button; the "Enviar emails agora" action only renders for a session that's already authenticated as admin.
- **Live score stuck showing a finished match as "in progress" all night (Eduardo):** root cause was the goal-scorer feature added in v4.53 — `extractGoalEvents()` ran unguarded inside `mapEspnToLiveScores()`, so any unexpected shape in ESPN's `details` array (e.g. a null entry) threw an exception before `_liveScores` got reassigned, freezing the live score/clock for every match until a page reload. Fixed in v4.55: the function can no longer throw, it just drops the goal-scorer list for that match on any unexpected data instead of blocking the score/clock update.
- **Automatic result emails need a full pipeline: live detection → confirmed over → double-checked → sent (Eduardo):** wanted a reliable listener linking the live-scoring feature to auto-sending result emails once a match is confirmed over, with the scoring double-checked as correct before anything goes out. Two things landed in v4.55: (1) restored the `auto_results.yml` cron schedule (every 10 min during match windows) that a previous session had swapped for a manual-only trigger — this is what actually and reliably detects "match over" server-side, independent of anyone having an admin browser tab open; (2) `send_result_email.py --auto` now re-fetches ESPN a second time, 20s after the first, and only saves+emails a result if both fetches agree exactly on the score and who advances — otherwise it skips and retries on the next cron tick.
- **Probability bar missing on the "Próximo jogo" hero card (Eduardo):** the pre-match probability bar existed on the Jogos tab and on the "ao vivo" hero card, but not on the hero's upcoming-match card shown when nothing is currently live — so there was no way to see the odds ahead of kickoff from the hero. Added in v4.55 via a shared `preMatchProbBarsHtml()`.
- **Goal scorer list should be split per team side, like Google's (Eduardo):** liked the artilheiro/minute feature (v4.53) but wanted it laid out like Google's live scoreboard — left team's goals on the left, right team's goals on the right, instead of one centered list. Changed in v4.56: two columns matching the team-name layout above (left = team A, right = team B, right column mirrored/right-aligned), flag icon per row dropped since the column position now makes the side obvious.
- **"Faca uma auditoria estilo big 4 nos resultados e no ranking" (Eduardo), triggered by a WhatsApp screenshot of a participant (Aline) confused about a rank change:** the confusion itself was already explained (M83 landed, see the entry above), but Eduardo asked for a full audit of the scoring/ranking pipeline and asked that this become standard practice before any future PR. Audit covered `app.js` (site) and `send_result_email.py` (cron email script) side by side. The site was clean throughout — everywhere (ranking, CSV/master export, admin's manual email builder) shares one `scoreEntry()` implementation, so it can't internally disagree with itself. The email script had drifted from it though, since it re-implements the scoring/bracket logic independently in Python:
  - **Critical**: its hardcoded R16/QF bracket pairing (`MATCH_TEAMS`) didn't match `data.js`'s real bracket for 9 of 16 matches (a copy/transcription error, unrelated to this session's edits) — would have caused ESPN lookups for those matches to silently never match anything once R16 started (2 days away at the time), stopping the auto-email pipeline for the rest of the tournament without any visible error.
  - **High**: the champion/runner-up/3rd/4th bonus points (25/15/10/5) were computed for tiebreak ordering only, never added to the email's point totals — the website already adds them. Would have surfaced as a site-vs-email total mismatch right as the tournament reached the final, the highest-stakes moment for a real-money pool.
  - **High**: 4th place was computed internally by two functions but dropped from what they returned, so its bonus could never actually be awarded even once the above was fixed.
  - **Low**: the Python score parser accepted negative/out-of-range values the browser's form already rejects — tightened to match exactly.
  All fixed in v4.57 and verified with automated tests: a programmatic diff of `MATCH_TEAMS` against `data.js` (now identical), and a full mock-tournament simulation with a "perfect bracket" entry confirming the champion resolves correctly through all 4 knockout rounds and the total (match points + bonus) matches what the site would compute.
- **Follow-up — make the audit automatic and permanent, not a one-time pass (Eduardo):** "this is the part of the site that can never be broken... run a full audit before an email is sent, right after the match finishes... every single change I ask you to do, review if scoring is still working, even if just to say nothing changed." Landed in v4.58: `bolao/scripts/audit_scoring.py` (new) packages the v4.57 audit's checks into a reusable suite; `send_result_email.py --auto` now runs it before touching anything and refuses to send any email if it fails, plus validates each individual result at runtime (event date not in the future, teams fully resolved, score/advanceSide shape sane) before trusting it — directly targeting the historical "result appeared for a match that hadn't happened" failure mode (see "Resultados somente do banco de dados" in `bolao/CHANGELOG.md`). Also added a standing rule to `CLAUDE.md`: every future change to this repo runs `audit_scoring.py` and reports the result, scoring-related or not.
- **Show stoppage time and halftime on the live clock, from a screenshot of Google's Portugal vs Croatia card (Eduardo):** "no tempo de jogo quando estiver acima de 45 coloque os acréscimos (+1, 2, 3...). No intervalo coloca que está no intervalo." Added in v4.59: the clock now shows "(+N)" once it passes 45/90/105/120 minutes (elapsed stoppage minutes, since ESPN's basic API doesn't expose the referee-announced allocation Google's number comes from), and shows "Intervalo" during halftime instead of a clock that kept climbing through the entire break — that was a real gap, not just cosmetic, since the interpolated running clock had no concept of "the game isn't actually being played right now." Applies to both the hero live card and the Jogos tab's status chip. Scoring untouched — audit re-run, still passes 5/5.
- **Follow-up — the clock was still running through Portugal vs Croatia's actual halftime (Eduardo), same day:** v4.59's fix relied on recognizing ESPN's halftime status text, which couldn't be confirmed against the real live feed from this sandbox (no network access to ESPN) — in practice it didn't match, and separately, a concurrent session's v4.61 (monotonic clock, no backward jumps) made this worse by removing the old 150s safety cap, so a genuinely paused clock with no matching status text would now extrapolate forward forever with nothing to stop it. Fixed in v4.62 with detection that doesn't depend on ESPN's wording at all: compares the raw match clock between two real polls, and if a lot of real time passed but the match clock barely moved, treats it as paused (halftime or any other stoppage) and freezes both the stored clock and the display until it genuinely starts moving again. Layered on top of v4.61's monotonic rewrite rather than replacing it. Verified with Playwright reproducing the exact scenario without any halftime-sounding status text — clock stays frozen through a full simulated break (well past where the old, now-removed 150s cap would have kicked in) and resumes correctly once real movement returns.
- **"O logo america 250 nao ficou legal, arrume ele ou ache outro oficial" + "os fireworks não aparecem" (Eduardo):** the America250 badge (added by a concurrent session, v4.67) was visually broken — root cause was a CSS selector bug: `.america250-badge span:first-child` meant to size only the flag emoji, but that combinator also matched `.a250-label` ("AMERICA") since it's independently the first child of its own parent, overriding its intended 7px with 17px and blowing up the whole layout (text collided with the stars, "250" wrapped to its own line). The fireworks report turned out not to be a bug — `launchJuly4Fireworks()` is correctly date-gated to July 4th only and it was still July 3rd; verified by forcing the browser clock forward that the canvas animation fires and renders correctly. Tried fetching the actual official America250 logo (Wikimedia Commons has an SVG) to replace the CSS recreation entirely, but this sandbox's network policy blocks that host (403). Eduardo then sent the official PNG directly — background removed programmatically (luminance-based alpha), cropped, and resized into `bolao/assets/america250-logo.png`; the badge now shows the real official mark instead of a CSS approximation. Fixed in v4.71.
- **Follow-up — "The logo is huge fix it this is horrible," with a photo of the actual live site (Eduardo):** the v4.71 fix looked correct in every test, and checking a clean, fully uncached load of the exact current `main` source confirmed the badge really does render at the intended small size — the deployed *source* was never broken. Real cause: a concurrent session pushed another commit (`021c1ae`, nav layout) about a minute after the badge fix merged, changing `styles.css` again but re-using the same `siteVersion` string ("v4.71") instead of bumping it — and `sync_version.yml` was scoped to only fire on `config.js` changes, so it never ran for that commit at all. `index.html`'s cache-busting `?v=` stayed frozen on a URL whose cached content no longer matched what a browser would need to load — that's what Eduardo's screenshot was actually showing. Fixed at the root in v4.72: `sync_version.yml` now triggers on any `bolao/js/**.js` or `bolao/css/**.css` change (not just `config.js`), and stamps `?v=` with the commit's short SHA instead of the manually-maintained `siteVersion` string, so a fresh cache-busting URL is guaranteed on every relevant push regardless of whether anyone remembers — or coordinates — a version bump. (`bolao/br2026/` has the same latent gap in its own `?v=` scheme, not yet covered by this workflow — left alone for now since it's unpublished and wasn't what was reported.)
- **"Bug no relógio... já estamos no minuto 54 e é o segundo tempo então +11 não está correto," with a screenshot of Australia vs Egypt showing "55:11 (+11)" (Eduardo):** real bug, confirmed. `formatMatchClock` had no upper limit on how far past a half boundary (45/90/105/120 min) it would still call the extra time "stoppage" — since ESPN's clock is one continuous counter with no built-in sense of "which half," a value like 55:11 (legitimately ~10 minutes into the 2nd half) kept reading as "45:00 plus 10 minutes of 1st-half stoppage" indefinitely, just because it was still numerically past 45. Fixed in v4.73 two ways: a realistic 8-minute cap on how long past any boundary something can still be called stoppage (beyond that, assume the half has changed and just show the plain clock), plus a more precise sticky per-match record — the moment the app directly observes the match paused (`isHalftime`) near a boundary, it remembers that boundary is behind us for good, so even resuming just 1–2 minutes later shows no false "(+N)" at all. Verified with Playwright reproducing the exact reported scores (55:11 → clean, no annotation) plus three regression cases (real 1st-half stoppage, resuming shortly after a confirmed halftime, real 2nd-half stoppage near 90) — all correct.
- **"deploy / Deploy GitHub Pages / deploy / Failed in 15 seconds" (Eduardo):** GitHub Actions deployment failure notification. Diagnosed via job logs: two deployment mechanisms were racing on every push — the automatic GitHub-managed "pages build and deployment" job (still active because the repo's Pages Source is set to "Deploy from a branch") and a custom `.github/workflows/pages.yml` added by a concurrent session (v4.69) using `actions/deploy-pages@v4`, which only works as the sole deploy path when Pages Source is "GitHub Actions." Both fired for the same commit at the same time, and one always lost with "Deployment failed, try again later." Presented Eduardo two fixes via AskUserQuestion — remove the redundant custom workflow (actionable immediately, no GitHub settings change needed) or switch the repo's Pages Source setting manually — he chose the former: "Eu removo o workflow customizado (Recomendado)." Fixed in v4.74 by deleting `.github/workflows/pages.yml`, restoring the single automatic deploy path that had worked reliably the whole time.
- **"The clock is still not working. Any better ideas on how to get as good as possible? Google has a good one. You can think harder if needed." (Eduardo):** every prior clock fix (v4.59, v4.62, v4.73) was a heuristic reconstruction — guessing which half a match was in purely from the raw, continuous `clockSeconds` number ESPN sends, with no built-in concept of "1st or 2nd half." That approach always had gaps: v4.73's 8-minute stoppage cap would hide a real 9+ minute injury-time allowance instead of showing it, its "confirmed halftime" memory only latched on if the app happened to poll within 3 minutes of the actual break, and extra time (105/120 min) never got the same protection. Root-cause fix in v4.75: ESPN's own scoreboard payload already includes `status.period` — a number stating definitively which half/period the match is in (1/2/3/4/5) — the same class of authoritative signal a broadcast-grade provider like the one behind Google's scoreboard would supply. `formatMatchClock` now uses `period` as the source of truth for the half boundary whenever ESPN provides it, correct the instant the period changes, no observation window needed and no need to hide real long stoppage. The old value-guessing logic is kept only as a fallback for the rare case `period` is absent from a payload — no regression risk. Verified with Playwright: the originally-reported scenario (period=2 from the very first poll, no halftime ever observed) shows clean immediately; a 9-minute 2nd-half injury-time allowance (previously hidden by the 8-min cap) now displays correctly; extra time no longer gets misread as 2nd-half stoppage; and the no-`period` fallback path still behaves exactly like v4.73.
- **Follow-up, with a screenshot of Google's own live card for the same match ("Australia vs Egypt... 1(0) — Live/Penalties — 1(0)", no clock at all): "It didn't stop when went to penalties. Look how google does it. Can you implement the same?" (Eduardo):** real gap — v4.75's period-based fix covered regulation and extra time but nothing treated a penalty shootout (`status.period === 5`) as a stoppage-in-play state, so the interpolated clock kept climbing indefinitely once the match reached penalties, since there's no real match clock running at that point at all. Fixed in v4.76 to match Google's own presentation: once penalties are detected (`period === 5`, or ESPN's status text as a fallback), the card shows a static "Pênaltis"/"Penales"/"Penalties" label instead of a clock, and the per-second interpolation is frozen — same treatment already used for halftime. Not yet implemented: the penalty-shootout score itself in parentheses next to the regular score (Google's "1 (0)") — couldn't confirm the exact ESPN field name for that without live network access to a real shootout, and didn't want to guess and risk showing a wrong number; can add it if Eduardo can share a live JSON sample from a shootout, or once it's confirmed another way.
- **Follow-up, "There's one live now that you can check" (Eduardo), then he pasted the real live ESPN JSON for that exact match after being told this sandbox has no network access to `site.api.espn.com` (confirmed 403 policy denial from the proxy's own logs — a real, unavoidable restriction, not something to route around):** the real payload proved v4.76's fix was incomplete. At the instant Australia vs Egypt finished extra time, `status.period` was still **4** (not 5, as guessed) and `status.type.name` was `"STATUS_END_OF_EXTRATIME"` with `detail`/`shortDetail` `"AET-pens"` — the clock (`status.clock`) was already frozen at 7200s (120:00). v4.76's detection only matched the literal word "penalt" in the status text, and "AET-pens" doesn't contain it, so this transitional state slipped through and the interpolated clock kept climbing. Fixed in v4.77: penalty detection now also matches `type.name` containing "END_OF_EXTRATIME" or "SHOOTOUT"/"PENALT", and the status text now also includes `type.detail` (previously only `description` + `shortDetail`), with the regex widened to catch "pens", "shootout", and "end of extra time" too. Also confirmed from the real payload: there's no separate shootout-score field present at this stage (still just the regular `score`), so the "1 (0)" display remains unimplemented, still pending a live sample from the actual shootout itself. Verified with Playwright using the exact real payload Eduardo sent — clock shows "Pênaltis" immediately and stays that way over several simulated minutes; all prior regression tests (v4.73/v4.75/v4.76) still pass.
- **Follow-up, screenshot showing "120:07 (+1)" and climbing while the phone's own live-activity banner confirmed penalties were actually in progress (Australia 2 vs Egypt 3 on kicks) (Eduardo), then a later screenshot after the shootout ended, "The penalties are done. Think harder for the next game on how to pull it.":** v4.77's fix caught the "AET-pens" transitional state, but once actual kicks started, ESPN reported some other status this app's text matching never got a sample of (the shootout finished before another JSON pull could be grabbed) — the clock resumed climbing until it self-corrected a couple minutes later. Rather than keep chasing exact ESPN wording one status at a time (fragile — there's no way to know every string ESPN might use for every micro-state without a live sample of each one), added a structural, wording-independent safety net: the `period`-based branch of `formatMatchClock` (introduced in v4.75) had **no cap at all**, unlike the older fallback branch, which always capped stoppage display at a realistic 8 minutes. For period 4 (extra time's 2nd half) specifically — the one period with no legitimate next real-clock state to grow into, since it's always followed by either full-time or a clockless shootout — the display now freezes for good at "120:00 (+8)" once that cap is reached, regardless of whether the status text is recognized. This bounds the worst case to a stable plateau instead of unbounded growth even if a future match hits yet another unrecognized ESPN status during this same transition. Verified with Playwright reproducing the exact reported value ("120:07 (+1)") and then simulating 20+ more minutes of "real" time passing with a never-before-seen status string — the display correctly plateaus at "128:00 (+8)" instead of climbing further. All prior regression tests (v4.73/v4.75/v4.76/v4.77) still pass.
- **"Retire esse botao" (Eduardo), after asking what the "Verificar reabertura" button in the reopen banner was:** explained it was just a manual `location.reload()` shortcut, redundant since the site already auto-checks every 60s for the cutoff update — Eduardo asked to remove it. Removed in v4.85, along with the now-unused `reopenCheckBtn` i18n key in all three languages. The "🔴 M88 ao vivo — aguardando resultado final" text stays; only the button is gone.
- **"Tem como fazer um visual melhor no mobile para a aba jogos? No pc parece ok…" (Eduardo), with a real phone screenshot:** three real issues, all from the same root cause. (1) Flag/team-name order zig-zagged between the two teams on mobile ("🇦🇺 Australia" / "Egypt 🇪🇬") — flag+name were a single concatenated text string, not separate elements, so CSS couldn't reorder them per breakpoint the way it needed to once both teams stack into one column. (2) The empty "×" placeholder for a not-yet-played match's score kept the same padded, 22px-font box used for real scores, reading as a large near-empty rectangle. (3) Probability-bar labels got clipped mid-word ("United State...", "gypt 17%") in the narrow 3-way split. Fixed in v4.86: flag and name are now separate `<span>`s (matching the pattern already used on the hero live card), letting mobile consistently show flag-first for both teams while desktop stays pixel-identical (verified via before/after screenshots at both breakpoints); the empty-score placeholder shrinks to plain small text on mobile only; prob-bar text now wraps instead of getting clipped. Asked to also check for the same class of bug elsewhere — found and fixed the identical issue in the bracket entry form (Palpites tab), which had no mobile layout rule at all and stayed cramped in the desktop's 3-column grid on any screen size; also aligned the admin "real results" panel to use the same markup for consistency. Verified with Playwright at 390px and 1280px, including a stress test with the bracket's longest team name ("Bosnia and Herzegovina").

---

## Feedback wishlist (not yet implemented)

These were discussed but not built. Documented here to avoid re-asking.

- Real-time multi-browser sync via Supabase Realtime subscriptions (currently only syncs on focus/visibility).
- Japanese (ja) language support — mentioned in early CLAUDE.md but never implemented in i18n.js.
- API-Football auto-update of bracket: when real match results come in, admin should be able to apply them to `data.js` without manual editing.
- A visual bracket tree view (bracket diagram) instead of match cards list.

---

## How to report a new bug

1. Open the app at `https://ferrarilabs.github.io/bolao-teste/`.
2. Open browser DevTools (F12) → Console tab → look for errors.
3. Send Eduardo the console output, browser/OS, and steps to reproduce via WhatsApp or email (emferrari@gmail.com).
