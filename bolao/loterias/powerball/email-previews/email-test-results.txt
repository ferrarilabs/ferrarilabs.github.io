Powerball email professionalization — admin test-send evidence (ROUND 4)
==========================================================================

Round 1: REJECTED (percentage mismatch, unreconciled financial summary,
fabricated correction diff).
Round 2: REJECTED (delivered subject was the EmailJS template's generic
fallback; ticket numbers rendered concatenated).
Round 3: subject and number-concatenation root-caused and fixed; Eduardo
then removed the static "Bolão do Ferrari - " EmailJS dashboard prefix
himself and asked for 3 more fixes before a final resend:
Round 4 (this round):
  1. Removed the redundant small gray plain-text line that appeared
     underneath the ball circles in the HTML version (it now lives ONLY in
     text/plain, as originally requested).
  2. Added a production attachment/link gate
     (validate.mjs::validateAttachmentsAndLinks) — blocks any REAL
     (non-test) publication send when the comprovante URL is empty,
     localhost, or a reserved/placeholder domain (.invalid/.example/.test/
     .local), or when a PDF/CSV/JSON attachment can't be resolved to a real
     URL or an existing file. Explicit error, not a silent skip. The
     fixture-driven admin TEST path is intentionally exempt (it uses
     example.invalid, a documented placeholder) — see
     scripts/email/publish_tickets.mjs's `if (!testMode)` gate.
  3. Restored/rebuilt the ball-circle visual design: searched git history
     (this branch + the separate powerball-professionalization-audit
     branch's reference "email outbox/pipeline/worker" implementation and
     its evidence HTML previews) for a prior richer ball design to reuse —
     none existed (that branch's previews have no ball rendering at all).
     Built fresh per spec: five fixed 32x32 light-gray circles + one larger
     36x36 solid-red Powerball circle, all six in the same row, true circles
     via matching width==height (not padding-stretched), "Power Play:
     Sim/Não" as its own small line below the row, no text label crowding
     the numbers. Applied to tickets-published AND tickets-corrected
     (BOTH the "Antes" and "Depois" ball rows in the diff box).

VERIFICATION METHODOLOGY: every claim below was cross-checked against the
actual delivered Gmail message (real search + full-content fetch), not
assumed from providerStatus:200.

HONEST NOTE ON THE "Bolão do Ferrari - " PREFIX: this round's 3 sends still
arrived with the "Bolão do Ferrari - " prefix despite Eduardo reporting he
removed it from the EmailJS dashboard. Possible causes: the dashboard change
had not fully propagated/saved before this round's sends, EmailJS-side
caching, or the removal was done after these specific sends went out. This
is flagged honestly rather than glossed over — Eduardo should visually
confirm in his own inbox whether the prefix is now gone or still present;
this codebase cannot control or verify the EmailJS dashboard's state.

FINAL RESEND — 3 emails, emferrari@gmail.com only
---------------------------------------------------

1) tickets-published
   emailJobId:          job_1785942557384_zwplnbkz
   Gmail message id:    19fd278c5b0d9878
   assunto real recebido (Gmail, verificado): Bolão do Ferrari - [TESTE ADMIN] 🎟️ Bilhetes publicados — Powerball de 05.08.2026 22:59 ET — 2 jogos
   destinatário:         emferrari@gmail.com
   sentAt (UTC):         2026-08-05T15:09:15.208Z
   providerStatus:       200
   Body verified (full HTML fetched and inspected): both games render as
   5 fixed 32x32 #f2f2f2 circles + 1 fixed 36x36 #CE1141 (red) circle, no
   gray fallback text line underneath, "Power Play: Sim" as its own line.

2) participant-added
   emailJobId:          job_1785942555251_ffcm2g7v
   Gmail message id:    19fd278bcfedf6b0
   assunto real recebido (Gmail, verificado): Bolão do Ferrari - [TESTE ADMIN] ✅ Participação confirmada — Bolão Powerball — Sorteio de 05.08.2026 22:59 ET
   destinatário:         emferrari@gmail.com
   providerStatus:       200
   (No ticket numbers in this template — unaffected by this round's ball-design change.)

3) tickets-corrected
   emailJobId:          job_1785942559249_5z3yci84
   Gmail message id:    19fd278cc1a422e3
   assunto real recebido (Gmail, verificado): Bolão do Ferrari - [TESTE ADMIN] ⚠️ Correção dos bilhetes — Powerball de 05.08.2026 22:59 ET — Versão 2
   destinatário:         emferrari@gmail.com
   providerStatus:       200
   Body verified: "O que foi alterado" box shows BOTH "Antes" and "Depois"
   as full ball-circle rows (not plain text) — Jogo 01's changed number (63
   -> 64) visibly different between the two circle rows, Powerball 17 circle
   unchanged in both.

All 3 sends recorded in scripts/email/outbox.json with testMode:true.
No real draw was marked as published. No real participant was notified.
Automated proof: node scripts/audit_email_tests.mjs (24/24),
scripts/audit_email_tests_round2.mjs (20/20),
scripts/audit_email_tests_round3.mjs (23/23) — 67/67 total, including new
tests for the attachment/link gate and the restored ball-circle design.
